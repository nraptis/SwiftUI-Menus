//
//  FrameDebuggerTool.swift
//  Jiggle3
//
//  Created by Nicky Taylor on 1/21/24.
//

import Foundation

/*
 [Elements - CreateSwatch]
 [SwiftUI - CreateSwatch]
 [View Models - CreateSwatch]
class FrameDebuggerTool {
    
    
    var lastFrame = [String]()
    var thisFrame = [String]()
    
    var didLogThisFrame = false
    
    func reset() {
        didLogThisFrame = false
        lastFrame = thisFrame
        thisFrame.removeAll(keepingCapacity: true)
        
    }
    
    func record(_ string: String) {
        
        if lastFrame.count > 0 {
            let index = thisFrame.count
            if index < lastFrame.count {
                if lastFrame[index] != string {
                    if didLogThisFrame == false {
                        print("mismatch on \(string)")
                        didLogThisFrame = true
                    }
                }
            }
        }
        thisFrame.append(string)
        
        
    }
    
    
    
    
}

let fd = FrameDebuggerTool()
*/

